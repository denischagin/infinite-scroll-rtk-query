import {createSlice} from "@reduxjs/toolkit";

const initialState = {

};
export const commentsPaginationSlice = createSlice({
    name: "comments-pagination",
    initialState,
    reducers: {

    }
})